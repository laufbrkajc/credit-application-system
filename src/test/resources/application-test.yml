spring:
  jpa:
    hibernate:
      ddl-auto: create
#  datasource:
#    url: jdbc:mariadb://${DB_HOST}:3306/${DB_NAME}
#    username: ${DB_USER}
#    password:
#    driver-class-name: org.mariadb.jdbc.Driver
  flyway:
    enabled: false